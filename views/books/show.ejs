<h2 class="page-header"><%= book.title %></h2>

<img width="100px" height="150px" src="<%= book.coverImagePath %>">
<div>Author: <%= book.author.name %></div>
<div>Publish Date: <%= book.publishDate.toDateString() %></div>
<div>Page Count: <%= book.pageCount %></div>
<div>Description: <%= book.description %></div>
<div class="form-row button-row">
    <a class="btn-primary" href="/books/<%= book.id %>/edit">Edit</a>
    <%- include('../partials/deleteForm.ejs', { url: `/books/${book.id}`}) %>
    <a class="btn-primary" href="/authors/<%= book.author.id %>">View Author</a>
</div>